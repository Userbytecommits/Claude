<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        INTERNET is only used for the optional one-time "Download model" fallback
        (ModelDownloader). Inference itself never makes a network call: once a .task
        file is loaded, everything runs on-device.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
        Lets "Find model on device" (ModelFileScanner) read other apps' Android/data
        folders - e.g. where Google's AI Edge Gallery stores its downloaded Gemma model -
        which a plain file picker often cannot browse into. Only requested when the user
        taps that button; the app works without it (just skip that button).
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />

    <application
        android:name=".AssistantApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.LocalAssistant"
        android:supportsRtl="true">

        <!-- Main chat UI, also handles being launched as the system Assist app -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.LocalAssistant">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            Registers this app as a selectable Assistant app (Settings > Apps > Default apps > Digital assistant app),
            the same mechanism Gemini/Google Assistant uses on Pixel.
        -->
        <service
            android:name=".assistant.AssistantSessionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
        </service>

        <service
            android:name=".assistant.AssistantSessionServiceImpl"
            android:exported="false" />

        <!--
            Accessibility service: lets the assistant read what is on screen and perform actions
            (tap, scroll, open Quick Settings tiles, open system Settings screens) on the user's behalf.
            Must be enabled manually by the user in Settings > Accessibility, Android does not allow
            silently granting this.
        -->
        <service
            android:name=".accessibility.DeviceControlAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/accessibility_service_label">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!--
            Notification listener: lets the assistant "read" incoming notifications (messages, etc.)
            so it can summarize/answer questions about them. Also needs manual user approval.
        -->
        <service
            android:name=".notifications.NotificationReaderService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
